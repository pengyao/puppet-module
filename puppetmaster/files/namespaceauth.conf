# This is an example namespaceauth.conf file,
# which you'll need if you want to start a client
# in --listen mode.
[fileserver]
    allow *.mobcon.inside

[puppetmaster]
    allow *.mobcon.inside

[puppetrunner]
    allow *.mobcon.inside

[puppetbucket]
    allow *.mobcon.inside

[puppetreports]
    allow *.mobcon.inside

[resource]
    allow puppet.mobcon.inside
